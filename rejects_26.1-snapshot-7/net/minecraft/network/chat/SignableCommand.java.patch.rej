++++ REJECTED HUNK: 1
@@ -22,7 +22,8 @@
         List<SignableCommand.Argument<S>> arguments = collectArguments(commandString, rootContext);
 
         CommandContextBuilder<S> child;
-        while ((child = context.getChild()) != null && child.getRootNode() != rootContext.getRootNode()) {
+        // Neo: Check if the command node is a RootCommandNode, instead of simply being the original root node; fixes #186
+        while ((child = context.getChild()) != null && !(child.getRootNode() instanceof com.mojang.brigadier.tree.RootCommandNode)) {
             arguments.addAll(collectArguments(commandString, child));
             context = child;
         }
++++ END HUNK
