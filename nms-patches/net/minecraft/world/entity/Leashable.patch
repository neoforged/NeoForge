--- a/net/minecraft/world/entity/Leashable.java
+++ b/net/minecraft/world/entity/Leashable.java
@@ -27,6 +27,11 @@
 import net.minecraft.world.phys.Vec3;
 import org.jspecify.annotations.Nullable;
 
+// CraftBukkit start
+import org.bukkit.event.entity.EntityUnleashEvent;
+import org.bukkit.event.entity.EntityUnleashEvent.UnleashReason;
+// CraftBukkit end
+
 public interface Leashable {
 
     String LEASH_TAG = "leash";
@@ -67,11 +72,11 @@
 
     default void setDelayedLeashHolderId(int entityId) {
         this.setLeashData(new Leashable.LeashData(entityId));
-        dropLeash((Entity) this, false, false);
+        dropLeash((Entity & Leashable) this, false, false); // CraftBukkit - decompile error
     }
 
     default void readLeashData(ValueInput input) {
-        Leashable.LeashData leashable_leashdata = (Leashable.LeashData) input.read("leash", Leashable.LeashData.CODEC).orElse((Object) null);
+        Leashable.LeashData leashable_leashdata = (Leashable.LeashData) input.read("leash", Leashable.LeashData.CODEC).orElse(null); // CraftBukkit - decompile error
 
         if (this.getLeashData() != null && leashable_leashdata == null) {
             this.removeLeash();
@@ -81,6 +86,14 @@
     }
 
     default void writeLeashData(ValueOutput output, Leashable.@Nullable LeashData leashData) {
+        // CraftBukkit start - SPIGOT-7487: Don't save (and possible drop) leash, when the holder was removed by a plugin
+        if (leashData != null) {
+            Entity entity = leashData.leashHolder;
+            if (entity != null && entity.pluginRemoved) {
+                return;
+            }
+        }
+        // CraftBukkit end
         output.storeNullable("leash", Leashable.LeashData.CODEC, leashData);
     }
 
@@ -106,7 +119,9 @@
                 }
 
                 if (entity.tickCount > 100) {
+                    entity.forceDrops = true; // CraftBukkit
                     entity.spawnAtLocation(serverlevel, (ItemLike) Items.LEAD);
+                    entity.forceDrops = false; // CraftBukkit
                     ((Leashable) entity).setLeashData((Leashable.LeashData) null);
                 }
             }
@@ -115,11 +130,11 @@
     }
 
     default void dropLeash() {
-        dropLeash((Entity) this, true, true);
+        dropLeash((Entity & Leashable) this, true, true); // CraftBukkit - decompile error
     }
 
     default void removeLeash() {
-        dropLeash((Entity) this, true, false);
+        dropLeash((Entity & Leashable) this, true, false); // CraftBukkit - decompile error
     }
 
     default void onLeashRemoved() {}
@@ -136,7 +151,9 @@
                 ServerLevel serverlevel = (ServerLevel) level;
 
                 if (dropLead) {
+                    entity.forceDrops = true; // CraftBukkit
                     entity.spawnAtLocation(serverlevel, (ItemLike) Items.LEAD);
+                    entity.forceDrops = false; // CraftBukkit
                 }
 
                 if (sendPacket) {
@@ -158,7 +175,8 @@
 
         if (leashable_leashdata != null && leashable_leashdata.leashHolder != null) {
             if (!entity.canInteractWithLevel() || !leashable_leashdata.leashHolder.canInteractWithLevel()) {
-                if ((Boolean) level.getGameRules().get(GameRules.ENTITY_DROPS)) {
+                level.getCraftServer().getPluginManager().callEvent(new EntityUnleashEvent(entity.getBukkitEntity(), (!entity.isAlive()) ? UnleashReason.PLAYER_UNLEASH : UnleashReason.HOLDER_GONE)); // CraftBukkit
+                if ((Boolean) level.getGameRules().get(GameRules.ENTITY_DROPS) && !entity.pluginRemoved) { // CraftBukkit - SPIGOT-7487: Don't drop leash, when the holder was removed by a plugin
                     ((Leashable) entity).dropLeash();
                 } else {
                     ((Leashable) entity).removeLeash();
@@ -210,6 +228,11 @@
     }
 
     default void leashTooFarBehaviour() {
+        // CraftBukkit start
+        if (this instanceof Entity entity) {
+            entity.level().getCraftServer().getPluginManager().callEvent(new EntityUnleashEvent(entity.getBukkitEntity(), EntityUnleashEvent.UnleashReason.DISTANCE));
+        }
+        // CraftBukkit end
         this.dropLeash();
     }
 
@@ -217,7 +240,7 @@
 
     default boolean checkElasticInteractions(Entity leashHolder, Leashable.LeashData leashData) {
         boolean flag = leashHolder.supportQuadLeashAsHolder() && this.supportQuadLeash();
-        List<Leashable.Wrench> list = computeElasticInteraction((Entity) this, leashHolder, flag ? Leashable.SHARED_QUAD_ATTACHMENT_POINTS : Leashable.ENTITY_ATTACHMENT_POINT, flag ? Leashable.SHARED_QUAD_ATTACHMENT_POINTS : Leashable.LEASHER_ATTACHMENT_POINT);
+        List<Leashable.Wrench> list = computeElasticInteraction((Entity & Leashable) this, leashHolder, flag ? Leashable.SHARED_QUAD_ATTACHMENT_POINTS : Leashable.ENTITY_ATTACHMENT_POINT, flag ? Leashable.SHARED_QUAD_ATTACHMENT_POINTS : Leashable.LEASHER_ATTACHMENT_POINT); // CraftBukkit - decompile error
 
         if (list.isEmpty()) {
             return false;
@@ -256,7 +279,7 @@
             Vec3 vec34 = entity.position().add(vec33);
             Vec3 vec35 = ((Vec3) leasherAttachmentPoints.get(i)).multiply(vec32).yRot(-f1);
             Vec3 vec36 = leashHolder.position().add(vec35);
-            Optional optional = computeDampenedSpringInteraction(vec36, vec34, d0, vec3, vec33);
+            Optional<Leashable.Wrench> optional = computeDampenedSpringInteraction(vec36, vec34, d0, vec3, vec33); // CraftBukkit - decompile error
 
             Objects.requireNonNull(list2);
             optional.ifPresent(list2::add);
@@ -313,7 +336,7 @@
 
     default void setLeashedTo(Entity holder, boolean synch) {
         if (this != holder) {
-            setLeashedTo((Entity) this, holder, synch);
+            setLeashedTo((Entity & Leashable) this, holder, synch); // CraftBukkit - decompile error
         }
     }
 
@@ -349,7 +372,7 @@
     }
 
     default @Nullable Entity getLeashHolder() {
-        return getLeashHolder((Entity) this);
+        return getLeashHolder((Entity & Leashable) this); // CraftBukkit - decompile error
     }
 
     private static <E extends Entity & Leashable> @Nullable Entity getLeashHolder(E entity) {
