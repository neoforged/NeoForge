--- a/net/minecraft/world/item/MobBucketItem.java
+++ b/net/minecraft/world/item/MobBucketItem.java
@@ -21,6 +21,12 @@
 import net.minecraft.world.phys.BlockHitResult;
 import org.jspecify.annotations.Nullable;
 
+// CraftBukkit start
+import net.minecraft.core.Direction;
+import net.minecraft.world.InteractionHand;
+import org.bukkit.event.entity.CreatureSpawnEvent;
+// CraftBukkit end
+
 public class MobBucketItem extends BucketItem {
 
     private final EntityType<? extends Mob> type;
@@ -57,19 +63,19 @@
         }
 
         if (mob != null) {
-            level.addFreshEntityWithPassengers(mob);
+            level.addFreshEntityWithPassengers(mob, CreatureSpawnEvent.SpawnReason.BUCKET); // CraftBukkit
             mob.playAmbientSound();
         }
 
     }
 
     @Override
-    public boolean emptyContents(@Nullable LivingEntity user, Level level, BlockPos pos, @Nullable BlockHitResult hitResult) {
+    public boolean emptyContents(@Nullable LivingEntity user, Level level, BlockPos pos, @Nullable BlockHitResult hitResult, Direction direction, BlockPos clicked, ItemStack itemstack, InteractionHand interactionhand) { // CraftBukkit
         if (this.content == Fluids.EMPTY) {
             this.playEmptySound(user, level, pos);
             return true;
         } else {
-            return super.emptyContents(user, level, pos, hitResult);
+            return super.emptyContents(user, level, pos, hitResult, direction, clicked, itemstack, interactionhand); // CraftBukkit
         }
     }
 
