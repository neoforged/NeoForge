--- a/net/minecraft/world/level/portal/PortalShape.java
+++ b/net/minecraft/world/level/portal/PortalShape.java
@@ -23,6 +23,11 @@
 import org.apache.commons.lang3.mutable.MutableInt;
 import org.jspecify.annotations.Nullable;
 
+// CraftBukkit start
+import org.bukkit.craftbukkit.util.BlockStateListPopulator;
+import org.bukkit.event.world.PortalCreateEvent;
+// CraftBukkit end
+
 public class PortalShape {
 
     private static final int MIN_WIDTH = 2;
@@ -40,8 +45,12 @@
     private final BlockPos bottomLeft;
     private final int height;
     private final int width;
+    // CraftBukkit start - add field
+    private final BlockStateListPopulator blocks;
 
-    private PortalShape(Direction.Axis axis, int portalBlockCount, Direction rightDir, BlockPos bottomLeft, int width, int height) {
+    private PortalShape(Direction.Axis axis, int portalBlockCount, Direction rightDir, BlockPos bottomLeft, int width, int height, BlockStateListPopulator blocks) {
+        this.blocks = blocks;
+        // CraftBukkit end
         this.axis = axis;
         this.numPortalBlocks = portalBlockCount;
         this.rightDir = rightDir;
@@ -69,43 +78,44 @@
     }
 
     public static PortalShape findAnyShape(BlockGetter level, BlockPos pos, Direction.Axis axis) {
+        BlockStateListPopulator blocks = new BlockStateListPopulator(((LevelAccessor) level).getMinecraftWorld()); // CraftBukkit
         Direction direction = axis == Direction.Axis.X ? Direction.WEST : Direction.SOUTH;
-        BlockPos blockpos1 = calculateBottomLeft(level, direction, pos);
+        BlockPos blockpos1 = calculateBottomLeft(level, direction, pos, blocks); // CraftBukkit
 
         if (blockpos1 == null) {
-            return new PortalShape(axis, 0, direction, pos, 0, 0);
+            return new PortalShape(axis, 0, direction, pos, 0, 0, blocks); // CraftBukkit
         } else {
-            int i = calculateWidth(level, blockpos1, direction);
+            int i = calculateWidth(level, blockpos1, direction, blocks); // CraftBukkit
 
             if (i == 0) {
-                return new PortalShape(axis, 0, direction, blockpos1, 0, 0);
+                return new PortalShape(axis, 0, direction, blockpos1, 0, 0, blocks); // CraftBukkit
             } else {
                 MutableInt mutableint = new MutableInt();
-                int j = calculateHeight(level, blockpos1, direction, i, mutableint);
+                int j = calculateHeight(level, blockpos1, direction, i, mutableint, blocks); // CraftBukkit
 
-                return new PortalShape(axis, mutableint.intValue(), direction, blockpos1, i, j);
+                return new PortalShape(axis, mutableint.intValue(), direction, blockpos1, i, j, blocks); // CraftBukkit
             }
         }
     }
 
-    private static @Nullable BlockPos calculateBottomLeft(BlockGetter level, Direction rightDir, BlockPos pos) {
+    private static @Nullable BlockPos calculateBottomLeft(BlockGetter level, Direction rightDir, BlockPos pos, BlockStateListPopulator blocks) { // CraftBukkit
         for (int i = Math.max(level.getMinY(), pos.getY() - 21); pos.getY() > i && isEmpty(level.getBlockState(pos.below())); pos = pos.below()) {
             ;
         }
 
         Direction direction1 = rightDir.getOpposite();
-        int j = getDistanceUntilEdgeAboveFrame(level, pos, direction1) - 1;
+        int j = getDistanceUntilEdgeAboveFrame(level, pos, direction1, blocks) - 1; // CraftBukkit
 
         return j < 0 ? null : pos.relative(direction1, j);
     }
 
-    private static int calculateWidth(BlockGetter level, BlockPos bottomLeft, Direction rightDir) {
-        int i = getDistanceUntilEdgeAboveFrame(level, bottomLeft, rightDir);
+    private static int calculateWidth(BlockGetter level, BlockPos pos, Direction rightDir, BlockStateListPopulator blocks) { // CraftBukkit
+        int i = getDistanceUntilEdgeAboveFrame(level, pos, rightDir, blocks); // CraftBukkit
 
         return i >= 2 && i <= 21 ? i : 0;
     }
 
-    private static int getDistanceUntilEdgeAboveFrame(BlockGetter level, BlockPos pos, Direction direction) {
+    private static int getDistanceUntilEdgeAboveFrame(BlockGetter level, BlockPos pos, Direction direction, BlockStateListPopulator blocks) { // CraftBukkit
         BlockPos.MutableBlockPos blockpos_mutableblockpos = new BlockPos.MutableBlockPos();
 
         for (int i = 0; i <= 21; ++i) {
@@ -114,6 +124,7 @@
 
             if (!isEmpty(blockstate)) {
                 if (PortalShape.FRAME.test(blockstate, level, blockpos_mutableblockpos)) {
+                    blocks.setBlock(blockpos_mutableblockpos, blockstate, 18); // CraftBukkit - lower left / right
                     return i;
                 }
                 break;
@@ -124,31 +135,33 @@
             if (!PortalShape.FRAME.test(blockstate1, level, blockpos_mutableblockpos)) {
                 break;
             }
+            blocks.setBlock(blockpos_mutableblockpos, blockstate1, 18); // CraftBukkit - bottom row
         }
 
         return 0;
     }
 
-    private static int calculateHeight(BlockGetter level, BlockPos bottomLeft, Direction rightDir, int width, MutableInt portalBlockCount) {
+    private static int calculateHeight(BlockGetter level, BlockPos bottomLeft, Direction rightDir, int width, MutableInt portalBlockCount, BlockStateListPopulator blocks) { // CraftBukkit
         BlockPos.MutableBlockPos blockpos_mutableblockpos = new BlockPos.MutableBlockPos();
-        int j = getDistanceUntilTop(level, bottomLeft, rightDir, blockpos_mutableblockpos, width, portalBlockCount);
+        int j = getDistanceUntilTop(level, bottomLeft, rightDir, blockpos_mutableblockpos, width, portalBlockCount, blocks); // CraftBukkit
 
-        return j >= 3 && j <= 21 && hasTopFrame(level, bottomLeft, rightDir, blockpos_mutableblockpos, width, j) ? j : 0;
+        return j >= 3 && j <= 21 && hasTopFrame(level, bottomLeft, rightDir, blockpos_mutableblockpos, width, j, blocks) ? j : 0; // CraftBukkit
     }
 
-    private static boolean hasTopFrame(BlockGetter level, BlockPos bottomLeft, Direction rightDir, BlockPos.MutableBlockPos pos, int width, int height) {
+    private static boolean hasTopFrame(BlockGetter level, BlockPos bottomLeft, Direction rightDir, BlockPos.MutableBlockPos pos, int width, int height, BlockStateListPopulator blocks) { // CraftBukkit
         for (int k = 0; k < width; ++k) {
             BlockPos.MutableBlockPos blockpos_mutableblockpos1 = pos.set(bottomLeft).move(Direction.UP, height).move(rightDir, k);
 
             if (!PortalShape.FRAME.test(level.getBlockState(blockpos_mutableblockpos1), level, blockpos_mutableblockpos1)) {
                 return false;
             }
+            blocks.setBlock(blockpos_mutableblockpos1, level.getBlockState(blockpos_mutableblockpos1), 18); // CraftBukkit - upper row
         }
 
         return true;
     }
 
-    private static int getDistanceUntilTop(BlockGetter level, BlockPos bottomLeft, Direction rightDir, BlockPos.MutableBlockPos pos, int width, MutableInt portalBlockCount) {
+    private static int getDistanceUntilTop(BlockGetter level, BlockPos bottomLeft, Direction rightDir, BlockPos.MutableBlockPos pos, int width, MutableInt portalBlockCount, BlockStateListPopulator blocks) { // CraftBukkit
         for (int j = 0; j < 21; ++j) {
             pos.set(bottomLeft).move(Direction.UP, j).move(rightDir, -1);
             if (!PortalShape.FRAME.test(level.getBlockState(pos), level, pos)) {
@@ -172,6 +185,10 @@
                     portalBlockCount.increment();
                 }
             }
+            // CraftBukkit start - left and right
+            blocks.setBlock(pos.set(bottomLeft).move(Direction.UP, j).move(rightDir, -1), level.getBlockState(pos), 18);
+            blocks.setBlock(pos.set(bottomLeft).move(Direction.UP, j).move(rightDir, width), level.getBlockState(pos), 18);
+            // CraftBukkit end
         }
 
         return 21;
@@ -185,12 +202,28 @@
         return this.width >= 2 && this.width <= 21 && this.height >= 3 && this.height <= 21;
     }
 
-    public void createPortalBlocks(LevelAccessor level) {
+    // CraftBukkit start - return boolean, add entity
+    public boolean createPortalBlocks(LevelAccessor level, Entity entity) {
+        org.bukkit.World bworld = level.getMinecraftWorld().getWorld();
+
+        // Copy below for loop
         BlockState blockstate = (BlockState) Blocks.NETHER_PORTAL.defaultBlockState().setValue(NetherPortalBlock.AXIS, this.axis);
 
         BlockPos.betweenClosed(this.bottomLeft, this.bottomLeft.relative(Direction.UP, this.height - 1).relative(this.rightDir, this.width - 1)).forEach((blockpos) -> {
+            blocks.setBlock(blockpos, blockstate, 18);
+        });
+
+        PortalCreateEvent event = new PortalCreateEvent((java.util.List<org.bukkit.block.BlockState>) (java.util.List) blocks.getList(), bworld, (entity == null) ? null : entity.getBukkitEntity(), PortalCreateEvent.CreateReason.FIRE);
+        level.getMinecraftWorld().getServer().server.getPluginManager().callEvent(event);
+
+        if (event.isCancelled()) {
+            return false;
+        }
+        // CraftBukkit end
+        BlockPos.betweenClosed(this.bottomLeft, this.bottomLeft.relative(Direction.UP, this.height - 1).relative(this.rightDir, this.width - 1)).forEach((blockpos) -> {
             level.setBlock(blockpos, blockstate, 18);
         });
+        return true; // CraftBukkit
     }
 
     public boolean isComplete() {
