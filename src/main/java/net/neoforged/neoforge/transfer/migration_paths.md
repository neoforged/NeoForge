##                                             

| IFluidHandler                                                   | IItemHandler                                                              | IResourceHandler\<T\>                                                                                                                                                  | Reason                                                                                                                                                                                                                                                                                                   |
|-----------------------------------------------------------------|---------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `int` getTanks()                                                | `int` getSlots()                                                          | `int` size()                                                                                                                                                           | We need to know how many tanks, slots, or other (referred to as `index`) subsets of the resource there are to properly iterate over. This is expected to be used for all of the following calls if a full snapshot is desired from the block.                                                            |
| `FluidStack` getFluidInTank(`int` tank)                         | `ItemStack` getStackInSlot(`int` slot)                                    | `T` getResource(`int` index)                                                                                                                                           | What resource was in the `index` to handle filtering or other logic.                                                                                                                                                                                                                                     |
|                                                                 |                                                                           | `int` getAmount(`int` index)                                                                                                                                           | This was part of the previous handlers with how `ItemStacks` and `FluidStacks` work, but since the `IResource` is an immutable snapshot of a their respective type, with no amount, we need a second method to understand how much is there in the `index`.                                              |
| `int` getTankCapacity(`int` tank)                               | `int` getSlotLimit(`int` slot)                                            | `int` getCapacity(`int` index)                                                                                                                                         | Typically used when printing values to a player of how much can be stored, as well as backing logic to get the current max storage amount of the resource.                                                                                                                                               |
|                                                                 |                                                                           | `int` getCapacity(`int` index, `T` resource)                                                                                                                           | Same as `getCapacity(int)` but with the added filter of a specific resource to target.                                                                                                                                                                                                                   |
| `boolean` isFluidValid(`int` tank, `FluidStack` stack)          | `boolean` isItemValid(`int` slot, `ItemStack` stack)                      | `boolean` isValid(`int` index, `T` resource)                                                                                                                           | Validation if a resource can be inserted (and only that check). This can be anything from a slot prevention, or even a recipe lookup to verify that the resource is allowed.                                                                                                                             |
|                                                                 | `ItemStack` insertItem(`int` slot, `ItemStack` stack, `boolean` simulate) | `int` insert(`int` index, `T` resource, `int` amount, `TransactionContext` transaction)                                                                                | Inserts a specific resource and amount into a targeted index. This typically done with minimal if any allocations hence the int.                                                                                                                                                                         |
| `int` fill(`FluidStack` resource, `FluidAction` action)         |                                                                           | `int` insert(`T` resource, `int` amount, `TransactionContext` transaction)                                                                                             | Inserts a specific resource and amount and allow the handler to determine where it should go. Still with minimal allocations.                                                                                                                                                                            |
|                                                                 | `ItemStack` extractItem(`int` slot, `int` amount, `boolean` simulate)     | `ResourceStack<T>` ResourceHandlerUtil.extractIndexedAny(`IResourceHandler<T>` handler, `int` index, `int` amount, `TransactionContext` transaction, `T` emptyResource | There is now a requirement of knowing your resource before extracting. The ResourceHandlerUtils have a method that can handle this case                                                                                                                                                                  |
| `FluidStack` drain(`int` maxDrain, `FluidAction` action)        |                                                                           | `ResourceStack<T>` ResourceHandlerUtils.extractAny(`IResourceHandler<T>` handler, `int` amount, `TransactionContext` transaction, `T` emptyResource)                   | There is now a requirement of knowing your resource before extracting. The ResourceHandlerUtils have a method that can handle this case                                                                                                                                                                  |
| `FluidStack` drain(`FluidStack` resource, `FluidAction` action) |                                                                           | `int` extract(`T` resource, `int` amount, `TransactionContext` transaction)                                                                                            | Extracts a specific resource and amount allowing the handler to determine where it gathered the amounts.                                                                                                                                                                                                 |
|                                                                 |                                                                           | `int` extract(`int` index, `T` resource, `int` amount, `TransactionContext` transaction)                                                                               | Extracts a specific resource and amount into a targeted index. This typically done with minimal if any allocations hence the int.                                                                                                                                                                        |
|                                                                 |                                                                           | `boolean` allowsInsertion(int index)                                                                                                                                   | A structurally permanent* indication of if the handler can be inserted into the specified index. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `insert` |
|                                                                 |                                                                           | `boolean` allowsInsertion()                                                                                                                                            | A structurally permanent* indication of if the handler can be inserted at all. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `insert`                   |
|                                                                 |                                                                           | `boolean` allowsExtraction(int index)                                                                                                                                  | A structurally permanent* indication of if the handler can be extract into the specified index. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `extract` |
|                                                                 |                                                                           | `boolean` allowsExtraction()                                                                                                                                           | A structurally permanent* indication of if the handler can be extract at all. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `extract`                   |

*Structurally permanent
: Anything that once a capability has been created, should not change without invalidation.
If in doubt, return true
    
    
    
    