Essentially a todo list and some notes:
- First, I've commented things throughout what I've noticed may either need to be changed or already changed with "ADRIAN&SOARYN"
    I'll remove these (or you can), but this was to help quickly find things that don't need to persist after PR work.


- Things to look over:
    > Aggregate and other wrappers may need to have some polish.  As an example, old insert in AggregateResourceHandler would never really work would it? You'd get to the first one that returned 0 and just stop.
          We may want to remake those wrappers from scratch.
    > MutableResourceStack - Provides a mutable quantity for slots
    > Reworked ResourceStorageContents to use said mutable, but I am still thinking we should just remove the class/record and just make it be a NotNullList that lives on the ResourceStorage
    > Renamed VoidHandler to ResourceVoidHandler
    > The HandlerUtil's checks for is Full I think would lead to faulty results. Just wanted to note this just incase.
    > We need to be sure to emphasize that allowsExtraction/Insertion are PURELY hints for callers, as I noticed a few places that were in the middle of logic.
        These methods are not intended to lock or be used as guards in functional logic as it would be checked needlessly multiple times. While we
        do need to check if we can actually insert or extract, obeying the return on that is really relevant.

        Example:
        I place pipe, I "attach" to the inventory and see that it has allowsExtraction but not insertion so then I can then and their remove that from my update group.
        Neither you nor I now need to worry about the return of allowsExtraction as it was stateless to begin with, so it shouldn't be changing. Now, on the premise,
        that I for some reason STILL call insert, you don't need to check that call's return specifically, but rather handle figuring out why it wasn't true to begin with.

        It is not required anyone call allowsInsertion, but we definitely need to be clear it is NOT changing functional aspects of the handler itself only loosely the communication between handlers.

