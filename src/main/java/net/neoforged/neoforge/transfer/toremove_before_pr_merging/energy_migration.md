| IEnergyStorage       | IEnergyHandler                                                   | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
|----------------------|------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| -                    | `size(int index)`                                                | To keep in-line with resource handlers there are now multiple "buffers" or indices on a `IEnergyHandler`. If you want the same implementation as the old `IEnergyStorage`, then it may be advantageous to look into `ISingleEnergyHandler` which essentially presets everything to the assumption there is only one buffer. This is ultimately to allow modders to provide or extract energy from a specific slot should their handling seek to be more advanced of a use case. |
| -                    | `insert(int index, int amount, TransactionContext transaction)`  | New to the handlers is the concept of an index, so we now can insert energy into a specific index or buffer should the consumer of the interface have the use-case. This is a fairly low maintenance burden and should a modder want to avoid indices all together they definitely have a builtin example of doing so should they not want to worry about it.                                                                                                                   |
| `receiveEnergy`      | `insert(int amount, TransactionContext transaction)`             | With the exception of the transaction, this should be roughly the same use case. The MAIN difference is that there is now a transaction chain that is passed in and used for snapshots which will require care.                                                                                                                                                                                                                                                                 |
| -                    | `extract(int index, int amount, TransactionContext transaction)` | New to the handlers is the concept of an index, so we now can extract energy from a specific index or buffer should the consumer of the interface have the use-case. This is a fairly low maintenance burden and should a modder want to avoid indices all together they definitely have a builtin example of doing so should they not want to worry about it.                                                                                                                  |
| `extractEnergy`      | `extract(int amount, TransactionContext transaction)`            | With the exception of the transaction, this should be roughly the same use case. The MAIN difference is that there is now a transaction chain that is passed in and used for snapshots which will require care.                                                                                                                                                                                                                                                                 |
| `getEnergyStored`    | `EnergyHandlerUtil.getAmount(handler)`                           | Keeping what was previously available, there is an "indexless" method of getting the current amount; do note that this iterates over the indices for you so while not truly index-less, you don't need to handle the iteration.                                                                                                                                                                                                                                                 |
| -                    | `getAmount(int index)`                                           | New indices (or size) require a way to access a particular energy buffer at a given slot. The old way of doing this still exists and should a developer not have additional buffers, would change essentially nothing for them.                                                                                                                                                                                                                                                 |
| `getMaxEnergyStored` | `EnergyHandlerUtil.getCapacity(handler)`                         | Keeping what was previously available, there is an "indexless" method of getting the capacity; do note that this iterates over the indices for you so while not truly index-less, you don't need to handle the iteration.                                                                                                                                                                                                                                                       |
| -                    | `getCapacity(int index`)                                         | New indices (or size) require a way to access a particular energy buffer at a given slot. The old way of doing this still exists and should a developer not have additional buffers, would change essentially nothing for them.                                                                                                                                                                                                                                                 |
| `canExtract`         | -                                                                | Control logic should be done in `extract` instead having a cross referenced method. Implementations of course can make one and use it the same way, but there is no longer an api level `canExtract` as this led to some relatively common mistakes in use. One notable one was not remembering to call it before inserting.                                                                                                                                                    | 
| `canReceive`         | -                                                                | Control logic should be done in `insert` instead of having a cross referenced method. Implementations of course can make one and use it the same way, but there is no longer an api level `canInsert` as this led to some relatively common mistakes in use. One notable one was not remembering to call it before inserting.                                                                                                                                                   |
| -                    | `supportsInsertion(int index)`                                   | A structurally permanent* indication of if the handler can be inserted into the specified index. If this value is expected to change at the given index throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `insert`                                                                                                                                                     |
| -                    | `supportsInsertion()`                                            | A structurally permanent* indication of if the handler can be inserted at all. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `insert`                                                                                                                                                                                          |
| -                    | `supportsExtraction(int index)`                                  | A structurally permanent* indication of if the handler can be extract into the specified index. If this value is expected to change at the given index throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `extract`                                                                                                                                                     |
| -                    | `supportsExtraction()`                                           | A structurally permanent* indication of if the handler can be extract at all. If this value is expected to change throughout the capability's lifetime, it is recommended to return true. This is **NOT** logic control, meaning that what ever returns here does not affect `extract`                                                                                                                                                                                          |

*Structurally permanent
: Anything that once a capability has been created, should not change without invalidation.
If in doubt, return true