--- a/net/minecraft/world/entity/animal/frog/Tadpole.java
+++ b/net/minecraft/world/entity/animal/frog/Tadpole.java
@@ -228,8 +_,10 @@
     }
 
     private void ageUp() {
+        if (!net.neoforged.neoforge.event.EventHooks.canLivingConvert(this, EntityType.FROG, (timer) -> {})) return;
         if (this.level() instanceof ServerLevel serverlevel) {
             this.convertTo(EntityType.FROG, ConversionParams.single(this, false, false), p_375845_ -> {
+                net.neoforged.neoforge.event.EventHooks.onLivingConvert(this, p_375845_);
                 p_375845_.finalizeSpawn(serverlevel, this.level().getCurrentDifficultyAt(p_375845_.blockPosition()), EntitySpawnReason.CONVERSION, null);
                 p_375845_.setPersistenceRequired();
                 p_375845_.fudgePositionAfterSizeChange(this.getDimensions(this.getPose()));
