--- a/net/minecraft/server/rcon/thread/RconClient.java
+++ b/net/minecraft/server/rcon/thread/RconClient.java
@@ -123,13 +_,14 @@
     }
 
     private void sendCmdResponse(int requestid, String response) throws IOException {
-        int len = response.length();
-
+        byte[] whole = response.getBytes(StandardCharsets.UTF_8);
+        int len = whole.length;
+        int start = 0;
         do {
             int dataLen = 4096 <= len ? 4096 : len;
-            this.send(requestid, 0, response.substring(0, dataLen));
-            response = response.substring(dataLen);
-            len = response.length();
+            this.send(requestid, 0, new String(java.util.Arrays.copyOfRange(whole, start, dataLen+start), StandardCharsets.UTF_8));
+            len -= dataLen;
+            start += dataLen;
         } while (0 != len);
     }
 
