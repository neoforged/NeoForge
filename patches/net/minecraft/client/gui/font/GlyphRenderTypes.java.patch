--- a/net/minecraft/client/gui/font/GlyphRenderTypes.java
+++ b/net/minecraft/client/gui/font/GlyphRenderTypes.java
@@ -10,27 +_,49 @@
 import net.neoforged.api.distmarker.OnlyIn;
 
 @OnlyIn(Dist.CLIENT)
-public record GlyphRenderTypes(RenderType normal, RenderType seeThrough, RenderType polygonOffset, RenderPipeline guiPipeline) {
+public record GlyphRenderTypes(RenderType normal, RenderType seeThrough, RenderType polygonOffset, RenderPipeline guiPipeline,
+                               // Neo: Allow linear filtering for in-world text
+                               RenderType normalBlur, RenderType seeThroughBlur, RenderType polygonOffsetBlur) {
+
+    /** @deprecated Neo: Use {@link GlyphRenderTypes(RenderType,RenderType,RenderType,RenderPipeline,RenderType,RenderType,RenderType)} instead */
+    @Deprecated
+    public GlyphRenderTypes(RenderType normal, RenderType seeThrough, RenderType polygonOffset, RenderPipeline guiPipeline) {
+        this(normal, seeThrough, polygonOffset, guiPipeline, normal, seeThrough, polygonOffset);
+    }
+
     public static GlyphRenderTypes createForIntensityTexture(Identifier p_468890_) {
         return new GlyphRenderTypes(
             RenderTypes.textIntensity(p_468890_),
             RenderTypes.textIntensitySeeThrough(p_468890_),
             RenderTypes.textIntensityPolygonOffset(p_468890_),
-            RenderPipelines.GUI_TEXT_INTENSITY
+            RenderPipelines.GUI_TEXT_INTENSITY,
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextIntensityFiltered(p_468890_),
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextIntensitySeeThroughFiltered(p_468890_),
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextIntensityPolygonOffsetFiltered(p_468890_)
         );
     }
 
     public static GlyphRenderTypes createForColorTexture(Identifier p_467758_) {
         return new GlyphRenderTypes(
-            RenderTypes.text(p_467758_), RenderTypes.textSeeThrough(p_467758_), RenderTypes.textPolygonOffset(p_467758_), RenderPipelines.GUI_TEXT
+            RenderTypes.text(p_467758_), RenderTypes.textSeeThrough(p_467758_), RenderTypes.textPolygonOffset(p_467758_), RenderPipelines.GUI_TEXT,
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextFiltered(p_467758_),
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextSeeThroughFiltered(p_467758_),
+            net.neoforged.neoforge.client.NeoForgeRenderTypes.getTextPolygonOffsetFiltered(p_467758_)
         );
     }
 
     public RenderType select(Font.DisplayMode p_285259_) {
+        return this.select(p_285259_, false);
+    }
+
+    /**
+     * Neo: returns the {@link RenderType} to use for the given {@link Font.DisplayMode} and blur setting
+     */
+    public RenderType select(Font.DisplayMode p_285259_, boolean blur) {
         return switch (p_285259_) {
-            case NORMAL -> this.normal;
-            case SEE_THROUGH -> this.seeThrough;
-            case POLYGON_OFFSET -> this.polygonOffset;
+            case NORMAL -> blur ? this.normalBlur : this.normal;
+            case SEE_THROUGH -> blur ? this.seeThroughBlur : this.seeThrough;
+            case POLYGON_OFFSET -> blur ? this.polygonOffsetBlur : this.polygonOffset;
         };
     }
 }
