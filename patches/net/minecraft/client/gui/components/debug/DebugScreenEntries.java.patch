--- a/net/minecraft/client/gui/components/debug/DebugScreenEntries.java
+++ b/net/minecraft/client/gui/components/debug/DebugScreenEntries.java
@@ -56,6 +_,11 @@
     public static final Identifier VISUALIZE_SKY_LIGHT_SECTIONS = register("visualize_sky_light_sections", new DebugEntryNoop());
     public static final Identifier CHUNK_SECTION_VISIBILITY = register("chunk_section_visibility", new DebugEntryNoop());
     public static final Map<DebugScreenProfile, Map<Identifier, DebugScreenEntryStatus>> PROFILES;
+    // Neo: Mutable map to allow updating without exposing publicly
+    // due to how immaculate checks for visiblity changes
+    // this has to be below 'PROFILES' otherwise
+    // it complains thinking we are making 'PROFILES' private
+    private static final Map<DebugScreenProfile, Map<Identifier, DebugScreenEntryStatus>> PROFILES_MUTABLE;
 
     private static Identifier register(String id, DebugScreenEntry entry) {
         return register(Identifier.withDefaultNamespace(id), entry);
@@ -107,6 +_,22 @@
             SIMPLE_PERFORMANCE_IMPACTORS,
             DebugScreenEntryStatus.IN_OVERLAY
         );
-        PROFILES = Map.of(DebugScreenProfile.DEFAULT, defaultProfile, DebugScreenProfile.PERFORMANCE, performance);
+        // Neo: Track initial profile values
+        PROFILES_MUTABLE = new HashMap<>();
+        PROFILES_MUTABLE.put(DebugScreenProfile.DEFAULT, defaultProfile);
+        PROFILES_MUTABLE.put(DebugScreenProfile.PERFORMANCE, performance);
+        PROFILES = java.util.Collections.unmodifiableMap(PROFILES_MUTABLE);
+    }
+
+    // Neo: Register modded debug entries and profile inclusions
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public static void registerModdedDebugEntries() {
+        if (net.neoforged.neoforge.client.ClientHooks.registeredModdedDebugEntries()) {
+            throw new IllegalStateException("Already registered modded debug entries!");
+        }
+
+        var event = new net.neoforged.neoforge.client.event.RegisterDebugEntriesEvent(ENTRIES_BY_ID, PROFILES_MUTABLE);
+        net.neoforged.fml.ModLoader.postEvent(event);
+        PROFILES_MUTABLE.putAll(event.validateProfiles());
     }
 }
