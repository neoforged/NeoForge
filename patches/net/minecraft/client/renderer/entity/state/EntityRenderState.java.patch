--- a/net/minecraft/client/renderer/entity/state/EntityRenderState.java
+++ b/net/minecraft/client/renderer/entity/state/EntityRenderState.java
@@ -7,7 +_,7 @@
 import net.neoforged.api.distmarker.OnlyIn;
 
 @OnlyIn(Dist.CLIENT)
-public class EntityRenderState {
+public class EntityRenderState extends net.neoforged.neoforge.attachment.AttachmentHolder implements net.neoforged.neoforge.client.entity.state.IRenderStateExtension {
     public double x;
     public double y;
     public double z;
@@ -27,6 +_,19 @@
     public Vec3 nameTagAttachment;
     @Nullable
     public EntityRenderState.LeashState leashState;
+    public float partialTick;
+    private java.util.Map<net.neoforged.neoforge.client.entity.state.RenderStateKey<?>, Object> extensions = new it.unimi.dsi.fastutil.objects.Reference2ObjectOpenHashMap<>(); // NeoForge: allow generic extension of render states
+
+    @Nullable
+    @Override
+    public <T> T getExtension(net.neoforged.neoforge.client.entity.state.RenderStateKey<T> key) {
+        return (T) extensions.get(key);
+    }
+
+    @Override
+    public <T> void setExtension(net.neoforged.neoforge.client.entity.state.RenderStateKey<T> key, T obj) {
+        extensions.put(key, obj);
+    }
 
     @OnlyIn(Dist.CLIENT)
     public static class LeashState {
