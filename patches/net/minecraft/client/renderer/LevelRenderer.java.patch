--- a/net/minecraft/client/renderer/LevelRenderer.java
+++ b/net/minecraft/client/renderer/LevelRenderer.java
@@ -226,6 +_,8 @@
             this.targets.alwaysOnTopDepth = frame.createInternal("always_on_top_depth", extraDepthTargetDescriptor);
         }
 
+        net.neoforged.neoforge.client.ClientHooks.fireFrameGraphSetup(frame, this.targets, cameraState, cameraState.viewRotationMatrix, profiler);
+
         this.targets.entityOutline = frame.importExternal("entity_outline", this.entityOutlineTarget);
         FramePass clearPass = frame.addPass("clear");
         this.targets.main = clearPass.readsAndWrites(this.targets.main);
@@ -252,7 +_,7 @@
             chunkSectionsToRender = this.prepareChunkRenders(terrainMatrix, !this.gameRenderer.useImprovedTransparency());
         }
 
-        this.addMainPass(frame, featureFrame, terrainFog, chunkSectionsToRender, consistentDepthRequired);
+        this.addMainPass(frame, featureFrame, terrainFog, chunkSectionsToRender, consistentDepthRequired, cameraState.viewRotationMatrix);
         if (this.currentFrameRendersEntityOutline) {
             PostChain entityOutlineChain = this.shaderManager.getPostChain(ENTITY_OUTLINE_POST_CHAIN_ID, LevelTargetBundle.OUTLINE_TARGETS);
             if (entityOutlineChain != null) {
@@ -319,6 +_,8 @@
             this.submitBlockOutline(poseStack, this.submitNodeStorage, levelRenderState);
         }
 
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.SubmitCustomGeometryEvent(levelRenderState, this.submitNodeStorage, poseStack, this.visibleSections));
+
         this.finalizeGizmoCollection();
         this.finalizedGizmos.standardPrimitives().submit(submitNodeCollector, levelRenderState.cameraRenderState, false);
         this.finalizedGizmos.alwaysOnTopPrimitives().submit(submitNodeCollector, levelRenderState.cameraRenderState, true);
@@ -358,6 +_,7 @@
                 this.targets.main = pass.readsAndWrites(this.targets.main);
                 pass.executes(
                     () -> {
+                        if (this.levelRenderState.customSkyboxRenderer == null || !this.levelRenderState.customSkyboxRenderer.renderSky(levelRenderState, state, cameraState.viewRotationMatrix, () -> RenderSystem.setShaderFog(skyFog))) {
                         RenderSystem.setShaderFog(skyFog);
                         if (state.skybox == DimensionType.Skybox.END) {
                             this.skyRenderer.renderEndSky();
@@ -377,12 +_,18 @@
                                 this.skyRenderer.renderDarkDisc();
                             }
                         }
+                        } // Neo: End of skip if custom sky is in effect
+                        Profiler.get().push("neoforge_render_after_sky");
+                        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterSky(this, this.levelRenderState, null, cameraState.viewRotationMatrix, this.visibleSections));
+                        Profiler.get().pop();
                     }
                 );
             }
         }
     }
 
+    /// @deprecated Neo: use [#addMainPass(FrameGraphBuilder, FeatureRenderDispatcher.PreparedFrame, GpuBufferSlice, ChunkSectionsToRender, boolean, Matrix4fc)] instead
+    @Deprecated
     private void addMainPass(
         FrameGraphBuilder frame,
         FeatureRenderDispatcher.PreparedFrame featureFrame,
@@ -390,6 +_,17 @@
         ChunkSectionsToRender chunkSectionsToRender,
         boolean consistentDepthRequired
     ) {
+        this.addMainPass(frame, featureFrame, terrainFog, chunkSectionsToRender, consistentDepthRequired, RenderSystem.getModelViewStack());
+    }
+
+    private void addMainPass(
+        FrameGraphBuilder frame,
+        FeatureRenderDispatcher.PreparedFrame featureFrame,
+        GpuBufferSlice terrainFog,
+        ChunkSectionsToRender chunkSectionsToRender,
+        boolean consistentDepthRequired,
+        Matrix4fc modelViewMatrix
+    ) {
         FramePass pass = frame.addPass("main");
         this.targets.main = pass.readsAndWrites(this.targets.main);
         boolean useImprovedTransparency = this.gameRenderer.useImprovedTransparency();
@@ -451,9 +_,9 @@
                             OptionalDouble.empty()
                         )) {
                     RenderSystem.bindDefaultUniforms(renderPass);
-                    this.executeSolid(chunkSectionsToRender, featureFrame, renderPass);
+                    this.executeSolid(chunkSectionsToRender, featureFrame, renderPass, modelViewMatrix);
                     if (!useImprovedTransparency) {
-                        this.executeClassicTransparency(chunkSectionsToRender, featureFrame, renderPass);
+                        this.executeClassicTransparency(chunkSectionsToRender, featureFrame, renderPass, modelViewMatrix);
                     }
                 }
 
@@ -514,15 +_,25 @@
         return !this.finalizedGizmos.alwaysOnTopPrimitives().isEmpty();
     }
 
+    /// @deprecated Neo: use [#executeSolid(ChunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame, RenderPass, Matrix4fc)] instead
+    @Deprecated
     private void executeSolid(ChunkSectionsToRender chunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame featureFrame, RenderPass renderPass) {
+        this.executeSolid(chunkSectionsToRender, featureFrame, renderPass, RenderSystem.getModelViewStack());
+    }
+
+    private void executeSolid(ChunkSectionsToRender chunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame featureFrame, RenderPass renderPass, Matrix4fc modelViewMatrix) {
         ProfilerFiller profiler = Profiler.get();
         profiler.push("solidTerrain");
         GpuTextureView blockAtlas = this.textureManager.getTexture(TextureAtlas.LOCATION_BLOCKS).getTextureView();
         chunkSectionsToRender.renderGroup(
             ChunkSectionLayerGroup.OPAQUE, renderPass, this.chunkLayerSampler, blockAtlas, this.levelRenderState.renderWireframeTerrain
         );
+        profiler.popPush("neoforge_render_after_opaque_blocks");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterOpaqueBlocks(this, levelRenderState, null, modelViewMatrix, this.visibleSections));
         profiler.popPush("renderSolidFeatures");
         featureFrame.executeSolid(renderPass);
+        profiler.popPush("neoforge_render_after_opaque_features");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterOpaqueFeatures(this, levelRenderState, null, modelViewMatrix, this.visibleSections));
         profiler.pop();
     }
 
@@ -665,29 +_,47 @@
         }
     }
 
+    /// @deprecated Neo: use [#executeClassicTransparency(ChunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame, RenderPass, Matrix4fc)] instead
+    @Deprecated
     private void executeClassicTransparency(
         ChunkSectionsToRender chunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame featureFrame, RenderPass renderPass
     ) {
+        this.executeClassicTransparency(chunkSectionsToRender, featureFrame, renderPass, RenderSystem.getModelViewStack());
+    }
+
+    private void executeClassicTransparency(
+        ChunkSectionsToRender chunkSectionsToRender, FeatureRenderDispatcher.PreparedFrame featureFrame, RenderPass renderPass, Matrix4fc modelViewMatrix
+    ) {
         ProfilerFiller profiler = Profiler.get();
         CloudStatus cloudStatus = this.optionsRenderState.cloudStatus;
         boolean shouldRenderClouds = cloudStatus != CloudStatus.OFF && ARGB.alpha(this.levelRenderState.cloudColor) > 0;
         int renderDistance = this.optionsRenderState.renderDistance * 16;
         profiler.push("renderTranslucentFeatures");
         featureFrame.executeTranslucent(renderPass);
+        profiler.popPush("neoforge_render_after_translucent_features");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterTranslucentFeatures(this, levelRenderState, null, modelViewMatrix, this.visibleSections));
         profiler.pop();
         GpuTextureView blockAtlas = this.textureManager.getTexture(TextureAtlas.LOCATION_BLOCKS).getTextureView();
         profiler.push("translucentTerrain");
         chunkSectionsToRender.renderGroup(
             ChunkSectionLayerGroup.TRANSLUCENT, renderPass, this.chunkLayerSampler, blockAtlas, this.levelRenderState.renderWireframeTerrain
         );
+        profiler.popPush("neoforge_render_after_translucent_blocks");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterTranslucentBlocks(this, levelRenderState, null, modelViewMatrix, this.visibleSections));
         profiler.pop();
         featureFrame.executeTranslucentAfterTerrain(renderPass);
+        profiler.push("neoforge_render_after_translucent_particles");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterTranslucentParticles(this, levelRenderState, null, modelViewMatrix, this.visibleSections));
+        profiler.pop();
         if (shouldRenderClouds) {
             this.cloudRenderer.render(cloudStatus, renderPass);
         }
 
         Vec3 cameraPos = this.levelRenderState.cameraRenderState.pos;
         this.weatherEffectRenderer.render(this.levelRenderState.weatherRenderState, renderPass);
+        Profiler.get().push("neoforge_render_after_weather");
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.RenderLevelStageEvent.AfterWeather(this, this.levelRenderState, null, modelViewMatrix, this.visibleSections));
+        Profiler.get().pop();
         this.worldBorderRenderer.render(this.levelRenderState.worldBorderRenderState, renderPass, cameraPos, renderDistance);
     }
 
@@ -929,10 +_,10 @@
             SectionRenderDispatcher.RenderSection section = this.viewArea.getRenderSection(state.sectionNode());
             if (rebuildSync) {
                 profiler.push("compileSectionSynchronously");
-                section.compileSync(state.region());
+                section.compileSync(state.region(), state.additionalRenderers());
                 profiler.pop();
             } else {
-                section.compileAsync(state.region());
+                section.compileAsync(state.region(), state.additionalRenderers());
             }
         }
 
@@ -990,7 +_,7 @@
                     poseStack.translate(state.blockState().getOffset(pos));
                     BlockStateModel model = this.modelManager.getBlockStateModelSet().get(state.blockState());
                     random.setSeed(state.blockState().getSeed(pos));
-                    model.collectParts(random, parts);
+                    model.collectParts(net.minecraft.client.renderer.block.BlockAndTintGetter.EMPTY, BlockPos.ZERO, state.blockState(), random, parts);
                     submitNodeCollector.submitBreakingBlockModel(poseStack, List.copyOf(parts), state.progress(), model.hasMaterialFlag(1));
                     parts.clear();
                     poseStack.popPose();
@@ -1002,6 +_,13 @@
     private void submitBlockOutline(PoseStack poseStack, SubmitNodeCollector submitNodeCollector, LevelRenderState levelRenderState) {
         BlockOutlineRenderState state = levelRenderState.blockOutlineRenderState;
         if (state != null) {
+            boolean cancel = false;
+            for (net.neoforged.neoforge.client.CustomBlockOutlineRenderer customRenderer : state.customRenderers()) {
+                cancel |= customRenderer.render(state, submitNodeCollector, poseStack, levelRenderState);
+            }
+            if (cancel) {
+                return;
+            }
             Vec3 cameraPos = levelRenderState.cameraRenderState.pos;
             BlockPos pos = state.pos();
             poseStack.pushPose();
