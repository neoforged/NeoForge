--- a/net/minecraft/client/renderer/block/BlockModelShaper.java
+++ b/net/minecraft/client/renderer/block/BlockModelShaper.java
@@ -17,8 +_,14 @@
         this.modelManager = modelManager;
     }
 
+    @Deprecated
     public Material.Baked getParticleMaterial(BlockState blockState) {
-        return this.getBlockModel(blockState).particleMaterial();
+        return getParticleMaterial(blockState, net.minecraft.world.level.EmptyBlockAndTintGetter.INSTANCE, net.minecraft.core.BlockPos.ZERO);
+    }
+
+    public Material.Baked getParticleMaterial(BlockState blockState, net.minecraft.world.level.BlockAndTintGetter level, net.minecraft.core.BlockPos pos) {
+        var model = this.getBlockModel(blockState);
+        return model.particleMaterial(level, pos, blockState);
     }
 
     public BlockStateModel getBlockModel(BlockState state) {
