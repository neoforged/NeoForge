--- a/net/minecraft/client/resources/model/geometry/BakedQuad.java
+++ b/net/minecraft/client/resources/model/geometry/BakedQuad.java
@@ -28,12 +_,18 @@
     long packedUV2,
     long packedUV3,
     Direction direction,
-    BakedQuad.MaterialInfo materialInfo
+    BakedQuad.MaterialInfo materialInfo,
+    net.neoforged.neoforge.client.model.quad.BakedNormals bakedNormals,
+    net.neoforged.neoforge.client.model.quad.BakedColors bakedColors
 ) {
     public static final int VERTEX_COUNT = 4;
     public static final int FLAG_TRANSLUCENT = 1;
     public static final int FLAG_ANIMATED = 2;
 
+    public BakedQuad(Vector3fc position0, Vector3fc position1, Vector3fc position2, Vector3fc position3, long packedUV0, long packedUV1, long packedUV2, long packedUV3, Direction direction, BakedQuad.MaterialInfo materialInfo) {
+        this(position0, position1, position2, position3, packedUV0, packedUV1, packedUV2, packedUV3, direction, materialInfo, net.neoforged.neoforge.client.model.quad.BakedNormals.UNSPECIFIED, net.neoforged.neoforge.client.model.quad.BakedColors.DEFAULT);
+    }
+
     public Vector3fc position(int vertex) {
         return switch (vertex) {
             case 0 -> this.position0;
@@ -60,6 +_,8 @@
     public @interface MaterialFlags {
     }
 
+    /// @param ambientOcclusion `false` to force-disable AO for this quad or `true` to use the behavior dictated by
+    ///                         [net.neoforged.neoforge.client.extensions.BlockStateModelPartExtension#ambientOcclusion].
     public record MaterialInfo(
         TextureAtlasSprite sprite,
         ChunkSectionLayer layer,
@@ -68,11 +_,26 @@
         RenderType itemGlintSpecialRenderType,
         int tintIndex,
         @Nullable Direction shadeDirectionOverride,
-        int lightEmission
+        int lightEmission,
+        boolean ambientOcclusion
     ) {
+        /// @deprecated Neo: use [#MaterialInfo(TextureAtlasSprite, ChunkSectionLayer, RenderType, RenderType, RenderType, int, Direction, int, boolean)] instead
+        @Deprecated
+        public MaterialInfo(TextureAtlasSprite sprite, ChunkSectionLayer layer, RenderType itemRenderType, RenderType itemGlintRenderType, RenderType itemGlintSpecialRenderType, int tintIndex, @Nullable Direction shadeDirectionOverride, int lightEmission) {
+            this(sprite, layer, itemRenderType, itemGlintRenderType, itemGlintSpecialRenderType, tintIndex, shadeDirectionOverride, lightEmission, true);
+        }
+
+        /// @deprecated Neo: use [#of(net.minecraft.client.resources.model.sprite.Material.Baked, com.mojang.blaze3d.platform.Transparency, int, Direction, int, boolean)] instead
+        @Deprecated
         public static BakedQuad.MaterialInfo of(
             Material.Baked material, Transparency transparency, int tintIndex, @Nullable Direction shadeDirectionOverride, int lightEmission
         ) {
+            return of(material, transparency, tintIndex, shadeDirectionOverride, lightEmission, true);
+        }
+
+        public static BakedQuad.MaterialInfo of(
+                Material.Baked material, Transparency transparency, int tintIndex, @Nullable Direction shadeDirectionOverride, int lightEmission, boolean ambientOcclusion
+        ) {
             ChunkSectionLayer layer = ChunkSectionLayer.byTransparency(transparency);
             RenderType itemRenderType;
             RenderType itemGlintRenderType;
@@ -90,7 +_,7 @@
             }
 
             return new BakedQuad.MaterialInfo(
-                material.sprite(), layer, itemRenderType, itemGlintRenderType, itemGlintSpecialRenderType, tintIndex, shadeDirectionOverride, lightEmission
+                material.sprite(), layer, itemRenderType, itemGlintRenderType, itemGlintSpecialRenderType, tintIndex, shadeDirectionOverride, lightEmission, ambientOcclusion
             );
         }
 
