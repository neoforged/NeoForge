--- a/net/minecraft/world/level/levelgen/feature/trunkplacers/TrunkPlacer.java
+++ b/net/minecraft/world/level/levelgen/feature/trunkplacers/TrunkPlacer.java
@@ -64,7 +64,7 @@
     protected static void setDirtAt(
         LevelSimulatedReader level, BiConsumer<BlockPos, BlockState> trunkSetter, RandomSource random, BlockPos pos, TreeConfiguration config
     ) {
-        if (config.forceDirt || !isDirt(level, pos)) {
+        if (!(((net.minecraft.world.level.LevelReader) level).getBlockState(pos).onTreeGrow((net.minecraft.world.level.LevelReader) level, trunkSetter, random, pos, config)) && (config.forceDirt || !isDirt(level, pos))) {
             trunkSetter.accept(pos, config.dirtProvider.getState(random, pos));
         }
     }
